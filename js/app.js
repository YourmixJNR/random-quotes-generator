import Typed from 'typed.js';

const typed = new Typed('#element', {
  strings: ['Let\'\s Generate Some Quotes', 'Click the Generate Button to Start', 'Stay Inspired and be Motivated'],
  typeSpeed: 90,
  backSpeed: 210,
  shuffle: false,
  smartBackspace: false,
  loop: true
});